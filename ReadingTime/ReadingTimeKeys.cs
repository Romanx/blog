namespace Blog
{
    /// <summary>
    /// Keys to accessing common names for metadata
    /// </summary>
    public static class ReadingTimeKeys
    {
        /// <summary>
        /// The root key for finding the reading time metadata of a document
        /// </summary>
        public const string ReadingTime = nameof(ReadingTime);

        public const string ReadingTimeWordsPerMinute = nameof(ReadingTimeWordsPerMinute);
    }
}